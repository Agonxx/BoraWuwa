export function UnfinishedNotice() {
  return (
    <div
      style={{
        marginTop: 24,
        background: 'var(--surface-2)',
        border: '1px dashed var(--border-subtle-strong)',
        borderRadius: 'var(--radius-card)',
        padding: 24,
        fontSize: 13,
        color: 'var(--text-dim-2)',
      }}
    >
      Guia em construção — volte em breve.
    </div>
  );
}
